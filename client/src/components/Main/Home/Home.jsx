import React from "react";

const Home = () => {
  return <>
  <h1>Crear nueva oferta</h1>
  <section>
  <button> Manualmente </button>
  <button> Subir factura </button>
  </section>
  </>;
};

export default Home;
